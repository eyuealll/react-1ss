/* you can create multiple context as you can or as your project need this is the store there is no data the next is create provider or context provider is centeral that allow access the data and feed the data into the component */
import React from "react"
const UserContext = React.createContext();/* creating UserContext as a context from react.createcontext method */

export default UserContext; 